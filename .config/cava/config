# Cava config for eww dashboard visualizer

[general]
framerate = 60
bars = 60
bar_width = 1
bar_spacing = 0

[input]
method = pipewire
source = auto

[output]
method = raw
raw_target = /tmp/cava.raw
data_format = ascii
ascii_max_range = 8

[smoothing]
monstercat = 0
waves = 0
noise_reduction = 0.77
