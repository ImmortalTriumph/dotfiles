# Cava config for eww dashboard visualizer

[general]
framerate = 60
bars = 50

[input]
method = pipewire
source = auto

[output]
method = raw
raw_target = /tmp/cava.raw
data_format = ascii

[smoothing]
monstercat = 0
waves = 0
noise_reduction = 0.77
